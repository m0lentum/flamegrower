# build artifacts and profiling data
/target
**/*.rs.bk
flamegraph.svg
perf.*

# irrelevant session data
*.tiled-session

# assets that have source files in asset-sources
# and can be generated from them with a script
assets/scenes/
